{
  "tableName": "CSS File Location",
  "headings": [
    {
      "name": "Parameter",
      "width": "10%"
    },
    {
      "name": "Default Type",
      "width": "15%"
    },
    {
      "name": "Description",
      "width": "75%"
    }
  ],
  "rows": [
    {
      "parameter": "`watch`",
      "default_type": "Array",
      "description": "Expects an array of directories in the project's root. The build will watch for changes in those directories when running `npm start`"
    },
    {
      "parameter": "`copy`",
      "default_type": "Array of Objects",
      "description": "Expects an array of objects, each consisting of a key-value pair of input and output directories. The input directories are relative to the project root directory, while the output directories are relative to the defined `main.output` directory."
    }
  ]
}
